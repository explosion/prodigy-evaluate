repos:
    - repo: local
      hooks:
          - id: ruff
            name: ruff
            entry: ruff
            args: [--fix, --exit-non-zero-on-fix]
            language: system
            types: [python]
          - id: black
            name: black
            entry: black prodigy/ tests/
            language: system
            types: [python]
            require_serial: true
            log_file: .black.log